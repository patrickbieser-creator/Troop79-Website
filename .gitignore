# Claude Code — agent and user-specific files
PROJECT.user.md
.claude/settings.local.json
.claude/agents/
.claude-project
Agents/

# Production DB backups — contain real family PII, never commit
backups/
