import { MediaManagerView } from './media-manager-view';

export const metadata = {
  title: 'Media Manager — Troop 79'
};

export default function MediaManagerPage() {
  return <MediaManagerView />;
}
